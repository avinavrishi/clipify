/**
 * App theming entry — preset-based multi-theme system.
 * Default UI: **Dark Default** (`dark-default`).
 */
export {
  createThemeForPreset,
  createAppTheme,
  OLIVE_MAIN,
  OLIVE_LIGHT,
  OLIVE_DARK,
} from "./createThemeForPreset";
